# Environment Configuration Example
# Copy this file to either .env.development or .env.production
# and update the values according to your environment

# API Configuration
REACT_APP_API_PROTOCOL=http              # Use 'https' for production
REACT_APP_BACKEND_HOST=localhost         # Your API domain
REACT_APP_BACKEND_PORT=5001             # Your API port
REACT_APP_WS_PROTOCOL=ws                # Use 'wss' for production
REACT_APP_ALLOWED_ORIGINS=["localhost"] # JSON array of allowed hosts

# Security Settings
REACT_APP_DEBUG_MODE=false              # Enable debug logging
REACT_APP_API_TIMEOUT=30000            # API timeout in milliseconds
REACT_APP_ENABLE_SECURITY_HEADERS=true # Enable additional security headers

# Notes:
# - REACT_APP_BACKEND_HOST should be 'localhost' for local development
# - For production, use your actual domain (e.g., 'api.yourservice.com')
# - Always use HTTPS/WSS in production
# - Set appropriate timeouts based on your API response times
